// Common decorators entrypoint
// Add shared decorators here (e.g., @CurrentUser) as needed.

export { CurrentUser } from './current-user.decorator';
export { Permissions } from './permissions.decorator';
